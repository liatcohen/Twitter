import React, { useState } from 'react'
import styled from "styled-components"

const Suggestions = styled.div`

`

function PeopleToFollow(props) {
    return (
        <Suggestions>People To Follow</Suggestions>
    )
}

export default PeopleToFollow
